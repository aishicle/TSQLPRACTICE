Joins

--Inner Join /--  equi join  == it retrieves only the matching records from the tables
-- outer Join
	-- left outer join == it retrieves all records from the left table and only the matching record from the right table
	--right outer join
	-- full outer join
--  cross join
--  self join
